#TODO How many ways can you reverse a string in Python?
# Write at least two functions that take a string as input and return the reversed string
# You must use different methods to reverse the string in each function
# You might try: slicing, loops, recursion, built-in functions, etc.